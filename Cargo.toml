[workspace]
members = [
  "air",
  "assembly",
  "core",
  "miden",
  "processor",
  "prover",
  "stdlib",
  "tools",
  "verifier"
]

[profile.release]
codegen-units = 1
lto = true

[profile.bench]
codegen-units = 1
lto = true
