[workspace]
members = [
  "air",
  "assembly",
  "core",
  "distaff",
  "examples",
  "processor",
  "verifier"
]

[profile.release]
codegen-units = 1
lto = true

[profile.bench]
codegen-units = 1
lto = true
